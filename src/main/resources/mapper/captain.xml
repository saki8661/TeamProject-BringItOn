<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.teamprojectbringiton.team.captain.CaptainRepository">

    <insert id="insert">
        insert into captain_tb(team_id, user_id)
        values (#{teamId}, #{userId});
    </insert>

</mapper>