<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
        namespace="com.example.teamprojectbringiton.matching.MatchingRepository">

    <insert id="insertMatching">
        insert into matching_tb(matching_status, reservation_id, match_user_id)
        values (#{matchingStatus}, #{reservationId}, #{matchUserId});
    </insert>


</mapper>